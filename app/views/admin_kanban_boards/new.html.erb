<h2><%= link_to l(:label_kanban_board_plural), admin_kanban_boards_path %> &#187; <%=l(:label_kanban_board_new)%></h2>

<%= labelled_form_for :resource, @resource, { :url => admin_kanban_boards_url } do |f| %>
  <%= render :partial => 'form', :locals => { :f => f } %>
  <%= submit_tag l(:button_create) %>
<% end %>
